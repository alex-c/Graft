using System;

namespace Graft.Algorithms.MinimumSpanningTree
{
    public static class Prim
    {
        public static IGraph<T> FindMinimumSpanningTree<T>(IGraph<T> graph) where T : IEquatable<T>
        {
            throw new NotImplementedException();
        }
    }
}
